import { View, Text } from "react-native";

export function Defense({navigation}) {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        gap: 25,
      }}>
      <Text>Defense</Text>
    </View>
  );
}